// OpenNext + Cloudflare adapter config. Generates a Worker bundle from
// the Next build output so the app can run on Cloudflare Workers with
// Next.js SSR, ISR, and middleware support. See
// https://opennext.js.org/cloudflare for the full reference.

import { defineCloudflareConfig } from '@opennextjs/cloudflare';

export default defineCloudflareConfig({});
